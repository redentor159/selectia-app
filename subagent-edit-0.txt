[
  {
    "AllowMultiple": false,
    "EndLine": 35,
    "ReplacementContent": "import {\n  BarChart,\n  Bar,\n  LineChart,\n  Line,\n  XAxis,\n  YAxis,\n  ZAxis,\n  CartesianGrid,\n  Tooltip as RechartsTooltip,\n  ResponsiveContainer,\n  Legend,\n  ScatterChart,\n  Scatter,\n  Cell,\n} from \"recharts\";",
    "StartLine": 22,
    "TargetContent": "import {\n  BarChart,\n  Bar,\n  LineChart,\n  Line,\n  XAxis,\n  YAxis,\n  CartesianGrid,\n  Tooltip as RechartsTooltip,\n  ResponsiveContainer,\n  Legend,\n} from \"recharts\";"
  },
  {
    "AllowMultiple": false,
    "EndLine": 248,
    "ReplacementContent": "  const providersInTimeline = useMemo(() => {\n    if (!data) return [];\n    const maxIiByProvider = new Map<string, number>();\n    for (const m of data.models) {\n      if (m.intelligenceIndex !== null && m.intelligenceIndex !== undefined) {\n        const current = maxIiByProvider.get(m.provider) || 0;\n        if (m.intelligenceIndex > current) {\n          maxIiByProvider.set(m.provider, m.intelligenceIndex);\n        }\n      }\n    }\n    return Array.from(maxIiByProvider.entries())\n      .sort((a, b) => b[1] - a[1]) // sort by max II descending\n      .slice(0, 6)\n      .map((entry) => entry[0]);\n  }, [data]);\n\n  // 3. Velocidad vs Ventana de Contexto (Scatter)\n  const contextSpeedData = useMemo(() => {\n    if (!data) return [];\n    return data.models\n      .filter((m) => m.contextWindow != null && m.contextWindow > 0 && m.speedTps != null)\n      .map((m) => ({\n        x: Math.log10(m.contextWindow!),\n        rawContext: m.contextWindow,\n        y: m.speedTps,\n        name: m.name,\n        provider: m.provider,\n        color: m.providerColor || \"#5e6ad2\",\n      }));\n  }, [data]);\n\n  // 4. Open Weights vs Propietario (Barra Apilada por Proveedor)\n  const openWeightsData = useMemo(() => {\n    if (!data) return [];\n    const groups = new Map<string, { open: number; closed: number }>();\n    for (const m of data.models) {\n      if (!groups.has(m.provider)) groups.set(m.provider, { open: 0, closed: 0 });\n      if (m.openWeights) {\n        groups.get(m.provider)!.open++;\n      } else {\n        groups.get(m.provider)!.closed++;\n      }\n    }\n    return Array.from(groups.entries())\n      .map(([provider, counts]) => ({\n        provider,\n        open: counts.open,\n        closed: counts.closed,\n        total: counts.open + counts.closed,\n      }))\n      .sort((a, b) => b.total - a.total);\n  }, [data]);\n\n  // 5. Coding vs Agentic Index (Scatter)\n  const codingAgenticData = useMemo(() => {\n    if (!data) return [];\n    return data.models\n      .filter((m) => m.codingIndex != null && m.agenticIndex != null)\n      .map((m) => ({\n        x: m.codingIndex,\n        y: m.agenticIndex,\n        name: m.name,\n        provider: m.provider,\n        color: m.providerColor || \"#5e6ad2\",\n      }));\n  }, [data]);\n\n  // 6. Velocidad vs Precio (Scatter)\n  const efficiencyData = useMemo(() => {\n    if (!data) return [];\n    return data.models\n      .filter((m) => m.priceInputUsd != null && m.priceInputUsd > 0 && m.speedTps != null)\n      .map((m) => ({\n        x: Math.log10(m.priceInputUsd!),\n        rawPrice: m.priceInputUsd,\n        y: m.speedTps,\n        name: m.name,\n        provider: m.provider,\n        color: m.providerColor || \"#5e6ad2\",\n      }));\n  }, [data]);",
    "StartLine": 232,
    "TargetContent": "  const providersInTimeline = useMemo(() => {\n    if (!data) return [];\n    const maxIiByProvider = new Map<string, number>();\n    for (const m of data.models) {\n      if (m.intelligenceIndex !== null && m.intelligenceIndex !== undefined) {\n        const current = maxIiByProvider.get(m.provider) || 0;\n        if (m.intelligenceIndex > current) {\n          maxIiByProvider.set(m.provider, m.intelligenceIndex);\n        }\n      }\n    }\n    return Array.from(maxIiByProvider.entries())\n      .sort((a, b) => b[1] - a[1]) // sort by max II descending\n      .slice(0, 6)\n      .map((entry) => entry[0]);\n  }, [data]);"
  },
  {
    "AllowMultiple": false,
    "EndLine": 571,
    "ReplacementContent": "          </ResponsiveContainer>\n        </CardContent>\n      </Card>\n\n      <div className=\"grid grid-cols-1 lg:grid-cols-2 gap-5\">\n        {/* Velocidad vs Contexto */}\n        <Card className=\"bg-[var(--bg-surface)] border-[var(--border-default)]\">\n          <CardHeader className=\"pb-3\">\n            <CardTitle className=\"text-base flex items-center gap-1.5\">\n              Velocidad vs Ventana de Contexto\n            </CardTitle>\n            <CardDescription className=\"text-xs\">\n              Contexto en escala logarítmica vs Tokens por segundo\n            </CardDescription>\n          </CardHeader>\n          <CardContent>\n            <ResponsiveContainer width=\"100%\" height={300}>\n              <ScatterChart margin={{ top: 10, right: 16, bottom: 24, left: 8 }}>\n                <CartesianGrid stroke=\"var(--border-default)\" strokeDasharray=\"3 3\" />\n                <XAxis\n                  type=\"number\"\n                  dataKey=\"x\"\n                  name=\"Contexto (log10)\"\n                  domain={[3, 7]}\n                  ticks={[3, 4, 5, 6, 7]}\n                  tick={{ fill: \"var(--text-secondary)\", fontSize: 11 }}\n                  tickFormatter={(v) => {\n                    const real = Math.pow(10, v);\n                    if (real >= 1000000) return `${(real / 1000000).toFixed(0)}M`;\n                    if (real >= 1000) return `${(real / 1000).toFixed(0)}K`;\n                    return String(real);\n                  }}\n                  axisLine={{ stroke: \"var(--border-default)\" }}\n                  tickLine={false}\n                  label={{ value: \"Ventana de Contexto (tokens)\", position: \"insideBottom\", offset: -10, fill: \"var(--text-secondary)\", fontSize: 11 }}\n                />\n                <YAxis\n                  type=\"number\"\n                  dataKey=\"y\"\n                  name=\"Velocidad (tok/s)\"\n                  tick={{ fill: \"var(--text-secondary)\", fontSize: 11 }}\n                  axisLine={{ stroke: \"var(--border-default)\" }}\n                  tickLine={false}\n                  width={40}\n                />\n                <RechartsTooltip\n                  cursor={{ strokeDasharray: \"3 3\", stroke: \"var(--border-strong)\" }}\n                  content={({ active, payload }) => {\n                    if (!active || !payload?.length) return null;\n                    const d = payload[0].payload;\n                    return (\n                      <div className=\"rounded-lg border border-[var(--border-strong)] bg-[var(--bg-elevated)] p-2.5 shadow-lg text-xs max-w-[220px]\">\n                        <div className=\"font-semibold text-[var(--text-primary)] mb-1\">{d.name}</div>\n                        <div className=\"text-[var(--text-secondary)] space-y-0.5\">\n                          <div>Contexto: <span className=\"num text-[var(--text-primary)]\">{d.rawContext >= 1000 ? `${d.rawContext / 1000}K` : d.rawContext}</span></div>\n                          <div>Velocidad: <span className=\"num text-[var(--text-primary)]\">{d.y} tok/s</span></div>\n                        </div>\n                      </div>\n                    );\n                  }}\n                />\n                <Scatter data={contextSpeedData} isAnimationActive={false}>\n                  {contextSpeedData.map((entry, i) => (\n                    <Cell key={i} fill={entry.color} fillOpacity={0.7} stroke={entry.color} />\n                  ))}\n                </Scatter>\n              </ScatterChart>\n            </ResponsiveContainer>\n          </CardContent>\n        </Card>\n\n        {/* Open Weights vs Propietario */}\n        <Card className=\"bg-[var(--bg-surface)] border-[var(--border-default)]\">\n          <CardHeader className=\"pb-3\">\n            <CardTitle className=\"text-base flex items-center gap-1.5\">\n              Open Weights vs Propietario\n            </CardTitle>\n            <CardDescription className=\"text-xs\">\n              Cantidad de modelos por proveedor según apertura de pesos\n            </CardDescription>\n          </CardHeader>\n          <CardContent>\n            <ResponsiveContainer width=\"100%\" height={300}>\n              <BarChart data={openWeightsData} margin={{ top: 10, right: 16, bottom: 40, left: 8 }}>\n                <CartesianGrid stroke=\"var(--border-default)\" strokeDasharray=\"3 3\" vertical={false} />\n                <XAxis\n                  dataKey=\"provider\"\n                  tick={{ fill: \"var(--text-secondary)\", fontSize: 10 }}\n                  axisLine={{ stroke: \"var(--border-default)\" }}\n                  tickLine={false}\n                  angle={-30}\n                  textAnchor=\"end\"\n                  height={60}\n                />\n                <YAxis\n                  tick={{ fill: \"var(--text-secondary)\", fontSize: 11 }}\n                  axisLine={{ stroke: \"var(--border-default)\" }}\n                  tickLine={false}\n                  width={30}\n                />\n                <RechartsTooltip\n                  cursor={{ fill: \"var(--bg-overlay)\" }}\n                  content={({ active, payload, label }) => {\n                    if (!active || !payload?.length) return null;\n                    return (\n                      <div className=\"rounded-lg border border-[var(--border-strong)] bg-[var(--bg-elevated)] p-2.5 shadow-lg text-xs\">\n                        <div className=\"font-semibold text-[var(--text-primary)] mb-1.5\">{label}</div>\n                        {payload.map((p, idx) => (\n                          <div key={idx} className=\"flex items-center gap-2\">\n                            <span className=\"h-2 w-2 rounded-full\" style={{ backgroundColor: p.color }} />\n                            <span className=\"text-[var(--text-secondary)]\">{p.name}</span>\n                            <span className=\"num text-[var(--text-primary)] ml-auto\">{p.value}</span>\n                          </div>\n                        ))}\n                      </div>\n                    );\n                  }}\n                />\n                <Legend wrapperStyle={{ fontSize: 11 }} iconType=\"circle\" />\n                <Bar dataKey=\"open\" stackId=\"a\" name=\"Open Weights\" fill=\"#4ea7fc\" />\n                <Bar dataKey=\"closed\" stackId=\"a\" name=\"Propietario\" fill=\"#eb5757\" />\n              </BarChart>\n            </ResponsiveContainer>\n          </CardContent>\n        </Card>\n\n        {/* Coding vs Agentic Index */}\n        <Card className=\"bg-[var(--bg-surface)] border-[var(--border-default)]\">\n          <CardHeader className=\"pb-3\">\n            <CardTitle className=\"text-base flex items-center gap-1.5\">\n              Coding Index vs Agentic Index\n            </CardTitle>\n            <CardDescription className=\"text-xs\">\n              Comparativa de habilidades de programación vs agentes autónomos\n            </CardDescription>\n          </CardHeader>\n          <CardContent>\n            <ResponsiveContainer width=\"100%\" height={300}>\n              <ScatterChart margin={{ top: 10, right: 16, bottom: 24, left: 8 }}>\n                <CartesianGrid stroke=\"var(--border-default)\" strokeDasharray=\"3 3\" />\n                <XAxis\n                  type=\"number\"\n                  dataKey=\"x\"\n                  name=\"Coding Index\"\n                  domain={[0, 100]}\n                  tick={{ fill: \"var(--text-secondary)\", fontSize: 11 }}\n                  axisLine={{ stroke: \"var(--border-default)\" }}\n                  tickLine={false}\n                  label={{ value: \"Coding Index\", position: \"insideBottom\", offset: -10, fill: \"var(--text-secondary)\", fontSize: 11 }}\n                />\n                <YAxis\n                  type=\"number\"\n                  dataKey=\"y\"\n                  name=\"Agentic Index\"\n                  domain={[0, 100]}\n                  tick={{ fill: \"var(--text-secondary)\", fontSize: 11 }}\n                  axisLine={{ stroke: \"var(--border-default)\" }}\n                  tickLine={false}\n                  width={30}\n                />\n                <RechartsTooltip\n                  cursor={{ strokeDasharray: \"3 3\", stroke: \"var(--border-strong)\" }}\n                  content={({ active, payload }) => {\n                    if (!active || !payload?.length) return null;\n                    const d = payload[0].payload;\n                    return (\n                      <div className=\"rounded-lg border border-[var(--border-strong)] bg-[var(--bg-elevated)] p-2.5 shadow-lg text-xs max-w-[220px]\">\n                        <div className=\"font-semibold text-[var(--text-primary)] mb-1\">{d.name}</div>\n                        <div className=\"text-[var(--text-secondary)] space-y-0.5\">\n                          <div>Coding Index: <span className=\"num text-[var(--text-primary)]\">{d.x}</span></div>\n                          <div>Agentic Index: <span className=\"num text-[var(--text-primary)]\">{d.y}</span></div>\n                        </div>\n                      </div>\n                    );\n                  }}\n                />\n                <Scatter data={codingAgenticData} isAnimationActive={false}>\n                  {codingAgenticData.map((entry, i) => (\n                    <Cell key={i} fill={entry.color} fillOpacity={0.7} stroke={entry.color} />\n                  ))}\n                </Scatter>\n              </ScatterChart>\n            </ResponsiveContainer>\n          </CardContent>\n        </Card>\n\n        {/* Velocidad vs Precio */}\n        <Card className=\"bg-[var(--bg-surface)] border-[var(--border-default)]\">\n          <CardHeader className=\"pb-3\">\n            <CardTitle className=\"text-base flex items-center gap-1.5\">\n              Eficiencia (Velocidad vs Precio)\n            </CardTitle>\n            <CardDescription className=\"text-xs\">\n              Precio USD/M tokens (log10) vs Tokens por segundo\n            </CardDescription>\n          </CardHeader>\n          <CardContent>\n            <ResponsiveContainer width=\"100%\" height={300}>\n              <ScatterChart margin={{ top: 10, right: 16, bottom: 24, left: 8 }}>\n                <CartesianGrid stroke=\"var(--border-default)\" strokeDasharray=\"3 3\" />\n                <XAxis\n                  type=\"number\"\n                  dataKey=\"x\"\n                  name=\"Precio USD/M (log10)\"\n                  domain={[-2, 2.5]}\n                  ticks={[-2, -1, 0, 1, 2]}\n                  tick={{ fill: \"var(--text-secondary)\", fontSize: 11 }}\n                  tickFormatter={(v) => {\n                    const real = Math.pow(10, v);\n                    if (real >= 10) return `$${real.toFixed(0)}`;\n                    if (real >= 1) return `$${real.toFixed(0)}`;\n                    return real.toFixed(2);\n                  }}\n                  axisLine={{ stroke: \"var(--border-default)\" }}\n                  tickLine={false}\n                  label={{ value: \"Precio Input USD/M (escala log)\", position: \"insideBottom\", offset: -10, fill: \"var(--text-secondary)\", fontSize: 11 }}\n                />\n                <YAxis\n                  type=\"number\"\n                  dataKey=\"y\"\n                  name=\"Velocidad (tok/s)\"\n                  tick={{ fill: \"var(--text-secondary)\", fontSize: 11 }}\n                  axisLine={{ stroke: \"var(--border-default)\" }}\n                  tickLine={false}\n                  width={40}\n                />\n                <RechartsTooltip\n                  cursor={{ strokeDasharray: \"3 3\", stroke: \"var(--border-strong)\" }}\n                  content={({ active, payload }) => {\n                    if (!active || !payload?.length) return null;\n                    const d = payload[0].payload;\n                    return (\n                      <div className=\"rounded-lg border border-[var(--border-strong)] bg-[var(--bg-elevated)] p-2.5 shadow-lg text-xs max-w-[220px]\">\n                        <div className=\"font-semibold text-[var(--text-primary)] mb-1\">{d.name}</div>\n                        <div className=\"text-[var(--text-secondary)] space-y-0.5\">\n                          <div>Precio: <span className=\"num text-[var(--text-primary)]\">${d.rawPrice.toFixed(2)}/M</span></div>\n                          <div>Velocidad: <span className=\"num text-[var(--text-primary)]\">{d.y} tok/s</span></div>\n                        </div>\n                      </div>\n                    );\n                  }}\n                />\n                <Scatter data={efficiencyData} isAnimationActive={false}>\n                  {efficiencyData.map((entry, i) => (\n                    <Cell key={i} fill={entry.color} fillOpacity={0.7} stroke={entry.color} />\n                  ))}\n                </Scatter>\n              </ScatterChart>\n            </ResponsiveContainer>\n          </CardContent>\n        </Card>\n      </div>\n    </div>",
    "StartLine": 569,
    "TargetContent": "          </ResponsiveContainer>\n        </CardContent>\n      </Card>\n    </div>"
  }
]